services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/monitor.db
      HEADLESS: "true"
    volumes:
      - monitor-data:/data

volumes:
  monitor-data:
